* Needs to include Thunder's Edge factions
* Timer needs to count down not up. Timer should keep counting negative
* Maybe can emit a sound when at configured times?
* User can select faction and colors - need to make color presets
* Timer for secondary strategy actions
* When everyone passes out of a round during action phase we should automatically move from action phase to status phase
* Status phase needs a timer per person
* Agenda phase also needs timers for after/when and also for voting
* Each person should be able to claim their spot so they can mark their turn as done. After claiming a seat there should be a button to unclaim a seat. Users should not be able to act for other users. An admin user should still be able to edit game state
* A pause button should exist. We will take breaks during the game
* Admin user should be able to reset the turn timer for someone
* Timer is per turn
* All data on actions should be stored in a sqlite database stored on disk
* Want to see how much time spent in each phase at end of game
* Want to see how much time spent on each player at end of game
* Config options for the following:
  * Turn timer length
  * Tactical action length
  * Agenda vote timer length
  * Agenda phase "after an agenda is revealed"/"when an agenda is revealed" timer length
  * Any other timers
